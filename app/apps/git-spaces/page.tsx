import { Typography, Box } from '@mui/material';

export default function GitSpacesPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        GitSpaces
      </Typography>
      <Typography variant="body1">
        This is the GitSpaces page.
      </Typography>
    </Box>
  );
}