import { Typography, Box } from '@mui/material';

export default function JobsSpacesPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Spaces Jobs
      </Typography>
      <Typography variant="body1">
        This is the Spaces Jobs page.
      </Typography>
    </Box>
  );
}