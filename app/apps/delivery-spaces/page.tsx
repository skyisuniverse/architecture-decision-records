import { Typography, Box } from '@mui/material';

export default function DeliverySpacesPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Delivery Spaces
      </Typography>
      <Typography variant="body1">
        This is the Delivery Spaces page.
      </Typography>
    </Box>
  );
}