import { Typography, Box } from '@mui/material';

export default function VideoSpacesPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Spaces Video
      </Typography>
      <Typography variant="body1">
        This is the Spaces Video page.
      </Typography>
    </Box>
  );
}