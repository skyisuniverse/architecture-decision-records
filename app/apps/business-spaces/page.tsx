import { Typography, Box } from '@mui/material';

export default function BusinessSpacesPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Spaces Business
      </Typography>
      <Typography variant="body1">
        This is the Spaces Business page.
      </Typography>
    </Box>
  );
}