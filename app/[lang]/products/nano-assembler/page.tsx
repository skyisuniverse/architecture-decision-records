import { Typography, Box } from '@mui/material';

export default function NanoAssemblerPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Nano-assembler page
      </Typography>
      <Typography variant="body1">
        This is the Nano-assembler page.
      </Typography>
    </Box>
  );
}